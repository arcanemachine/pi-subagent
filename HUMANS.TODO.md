# Human TODOs

1. [x] If `/new` is called, subagent list should be empty (currently shows subagents from previous session).
2. [x] View output of running agents
   - Implemented `/subagent show <id>` to watch a subagent in live widget
   - `/subagent hide <id>` to stop watching
   - `/subagent show-all` and `hide-all` for batch operations
   - Widget shows status, current tool, duration, and auto-updates
